#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int m, n;

for (m = 97; m <= 122; m++)
putchar(m);
for (n = 65; n <= 90; n++)
putchar(n);
putchar('\n');
return (0);
}
