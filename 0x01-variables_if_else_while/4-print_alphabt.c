#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int m;

for (m = 97; m <= 122; m++)
{
       if (m == 101 || m == 113)
{
}
else
{
putchar(m);
}
}
putchar('\n');
return (0);
}
