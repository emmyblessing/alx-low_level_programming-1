#include "holberton.h"
#include <stdio.h>

/**
 * main - check the code for Holberton School students.
 *
 * Return: Always 0.
 */
int _abs(int s){
 if(s<0){
     return (s*-1);
 }else{
     return(s);
 }
return 0;
}